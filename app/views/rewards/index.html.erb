<div>
  <h1>All Rewards [<%= link_to "New", new_reward_path %>]</h1>

  <br>

  <table>
    <thead>
    <th>Name</th>
    <th>Cost</th>
    <th>Options</th>
    </thead>
    <tbody>
    <% @rewards.each do |reward| %>
      <tr>
        <td><%= reward.name %></td>
        <td><%= reward.points %></td>
        <td><%= link_to "View", reward_path(reward) %> | <%= link_to "Edit", edit_reward_path(reward) %> | <%= link_to "Delete", reward_path(reward), method: :delete, data: { confirm: 'Are you sure you wish to delete this reward?' }, :remote => true, :class => 'delete_part' %></td>
      </tr>
    <% end %>
    </tbody>

  </table>
</div>