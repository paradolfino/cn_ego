<%= form_for @user do |f| %>
    <p><%= f.text_field :name, placeholder: "Enter a Name" %></p>
    <p><%= f.text_field :email, placeholder: "Enter an Email" %></p>
    <p><%= f.password_field :password, placeholder: "Enter a Password" %></p>
    <%= f.submit "Update" %>
<% end %>
